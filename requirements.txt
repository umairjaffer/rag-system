# ============================================================
# Requirements for RAG System
# Install with: pip install -r requirements.txt
#
# Key packages:
#   - fastapi + uvicorn: async web framework and ASGI server
#   - langchain stack: LLM orchestration, FAISS, Groq, HuggingFace
#   - sentence-transformers: local embedding model
#   - pypdf: PDF text extraction
#   - python-dotenv: .env file loading
#   - pydantic-settings: typed settings from environment variables
# ============================================================

# Web Framework
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9

# LangChain Core
langchain>=0.3.0
langchain-core>=0.3.0
langchain-community>=0.3.0

# Groq LLM Integration (official LangChain partner package)
langchain-groq>=0.2.0

# HuggingFace Embeddings (official LangChain partner package)
langchain-huggingface>=0.1.0
sentence-transformers>=3.0.0

# Vector Store
faiss-cpu>=1.8.0

# PDF Loading
pypdf>=4.0.0

# Settings and environment
pydantic>=2.0.0
pydantic-settings>=2.0.0
python-dotenv>=1.0.0

# HTTP client (used internally by LangChain and Groq)
httpx>=0.27.0